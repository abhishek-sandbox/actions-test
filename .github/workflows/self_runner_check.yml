name: self runner github test

on:
  pull_request:
    branches: [ main ]

jobs:
  Build:
    runs-on: self-hosted

    steps:

    - name: Running on Personal runner
      run: echo "Hello Actions from Personal Runner!!"
